name: Deploy to Glitch

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Sync with Glitch
      run: |
        git remote add glitch https://api.glitch.com/git/somsoc
        git push glitch main --force
